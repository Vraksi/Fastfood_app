export class Accessory{

    public id: number;
    public name: string;
    public tag: string;

    constructor(id, name, tag){
        this.id = id
        this.name = name
        this.tag = tag
    }
}